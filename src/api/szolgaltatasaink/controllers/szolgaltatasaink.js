'use strict';

/**
 * szolgaltatasaink controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::szolgaltatasaink.szolgaltatasaink');
